paths:
  /api/productos:
  post:
    summary: Creates a new product
    tags:
      - Productos
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schema/newProducto"
    responses:
      "201":
        description: The created product.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Producto"
